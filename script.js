function showFireworks() {
    // You can add code here to display fireworks or any other animation
    // For simplicity, let's just change the background color after 2 seconds
    setTimeout(() => {
        document.body.style.backgroundColor = '#111';
    }, 2000);
}
